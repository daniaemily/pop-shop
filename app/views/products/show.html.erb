
<div class="container">
  <div class="show-title">
    <h1><%= @product.title %></h1>
    <p> Displays a specific product </p>
  </div>
  <div>
  </div>
  <div class="show-essential">
    <div class="show-photo">
      <% if @product.photo.present? %>
        <%= cl_image_tag @product.photo, height: 400, width: 500, crop: :fill %>
      <% end %>
      <p>
        <% @product.photo %>
      </p>
    </div>
    <div class="product-description">
      <p>
        <%= @product.description %>
      </p>
      <p>$
        <%= @product.asking_price %>
      </p>
    </div>

    <!-- Modal -->
    <!-- Button trigger modal -->
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
      Purchase
    </button>

    <!-- Modal -->
    <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLongTitle">Give me that Paper Paper Paper</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <p><%= @product.title %></p>
            <p><%= @product.asking_price %></p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="button" class="btn btn-primary">Save changes</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.onready(function() {
    console.log("Starbucks Wifi")
  })
</script>


